import { APIError } from '../../errors';
import type { APIRequest } from '../request';
import { APIWorker } from './worker';
export declare class SequentialWorker extends APIWorker {
    protected execute(request?: APIRequest | undefined): Promise<void>;
    /**
     * Error API handler
     */
    handleError(request: APIRequest, error: APIError): Promise<void>;
}
