import type { ExecuteError } from '../errors';
import { type API, APIRequest } from '../api';
export interface IExecutesPayload {
    response: any[];
    errors: ExecuteError[];
}
export interface IExecutesOptions {
    api: API;
    method: string;
    queue: Record<string, any>[];
}
export declare const executeRequests: (api: API, queue: APIRequest[]) => Promise<IExecutesPayload>;
export declare const executes: ({ api, method, queue }: IExecutesOptions) => Promise<IExecutesPayload>;
